{
  "name": "bhairava-workspace",
  "private": true,
  "description": "Current product: MAIN app. Older Admin / Agent / Customer portals are in archive/.",
  "scripts": {
    "dev": "npm run dev --prefix \"MAIN app\"",
    "dev:admin": "npm run dev --prefix archive/bhairava-app",
    "dev:agent": "npm run dev --prefix archive/bhairava-agent",
    "dev:customer": "npm run dev --prefix archive/bhairava-customer",
    "dev:all": "npx concurrently -n admin,agent,customer -c blue,green,magenta \"npm run dev:admin\" \"npm run dev:agent\" \"npm run dev:customer\"",
    "db:generate": "npm run db:generate --prefix archive/bhairava-app && node scripts/sync-prisma-client.cjs"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
