# Archived apps

These are the older Next.js portals. They are **not** used by the live demo.

The current product is `MAIN app/`.

| Folder | What it was |
| --- | --- |
| `bhairava-admin` | Older admin UI |
| `bhairava-app` | Older admin + Prisma schema |
| `bhairava-agent` | Agent portal |
| `bhairava-customer` | Customer portal |

Kept here so the work is not lost. Do not run these for the Monday demo.
